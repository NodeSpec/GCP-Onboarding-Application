{
  "name": "gcp-onboarding-application",
  "version": "0.1.0",
  "private": true,
  "description": "Serverless Google Workspace user-lifecycle console. Workspace root.",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "workspaces": [
    "packages/*",
    "services/*"
  ],
  "scripts": {
    "build": "npm run build --workspace @lifecycle/shared && npm run build --workspaces --if-present",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:emulator": "firebase emulators:exec --only firestore --project demo-lifecycle 'vitest run --config vitest.emulator.config.ts'",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "verify": "npm run build && npm run typecheck && npm run test && npm run test:emulator"
  },
  "devDependencies": {
    "firebase-tools": "^15.27.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.5"
  }
}
