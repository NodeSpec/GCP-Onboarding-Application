# Test Plan: REQ-010 - Audit event model, operator-action auditing and log redaction

## Testing Objectives

**Requirement:** Audit event model, operator-action auditing and log redaction
**Category:** non-functional
**Description:** Owned by the Lifecycle API Service, which also owns the shared audit and logging library both services use. Every operator action — approval, rejection, cancellation, resume, credential retrieval, role-binding change — and every authorization failure writes an append-only audit event carrying event id, request id, step id, actor identity (from the verified IAP assertion, or the system principal for automated steps), action, before/after state, target user, outcome, and timestamp. Audit events are written in the same Firestore transaction as the state change they describe.

Append-only in Firestore is enforced by the data access layer, which exposes no update or delete path for the audit collection. Note explicitly that Firestore security rules do NOT apply here, because all access is server-side through Admin SDK credentials which bypass rules entirely, and Firestore IAM is database-scoped rather than per-collection, so neither is a real control. Tamper-evident retention is REQ-018. Worker-side transition auditing is carried by REQ-016.

The redaction filter lives here too: secrets, passwords, ciphertext, and raw JWT assertions never reach any log sink.

## Project Context

Company wants an application that supports the full user lifecycle in four phases:

1. User creation, including assignment of attributes and groups
2. Notifying the user of their new account with a welcome letter and initial password setting instructions
3. Updating a user's roles/attributes over time
4. Account deletion

Scope and constraints as stated by the customer:

- The application should o …

## Acceptance Criteria

Criterion text lives HERE once; every section below cites the AC id.

- **AC-REQ-010-1** [automated] [VERIFIED] Each audit event records actor identity, action, target user, before/after state, outcome, and timestamp
- **AC-REQ-010-2** [automated] [VERIFIED] When an operator performs an approve, reject, cancel, resume, credential retrieval, or role-binding change, the API shall write the audit event and the state change it describes in a single Firestore transaction, verified by a test that forces the transaction to fail and observes neither the state change nor the audit event
- **AC-REQ-010-3** [automated] [VERIFIED] When a request is refused with 401 by assertion verification, or with 403 by the self-approval guard or a role check, the API shall write an audit event carrying the refusal reason, the requested path, and the source IP, recording actor identity only where verification succeeded
- **AC-REQ-010-4** [automated] [VERIFIED] When an automated step writes an audit event, the API shall record the actor as the system principal and shall also record the originating human requester of the parent request, so no machine action is left unattributable
- **AC-REQ-010-5** [automated] [VERIFIED] The data access layer exposes no update or delete operation against the audit collection, verified by a test asserting the module's public surface and by a repository check for direct Firestore delete calls on that collection
- **AC-REQ-010-6** [automated] [VERIFIED] A structured-logging redaction filter strips password, ciphertext, key, secret, token and assertion fields from every sink, verified by a test that logs a payload containing each and asserts on the emitted record
- **AC-REQ-010-7** [automated] [VERIFIED] When any audit event is written, the API shall persist a payload containing no password, no ciphertext, no secret value, and no raw JWT assertion, verified by a test that drives every audit-writing path and asserts on the persisted documents
- **AC-REQ-010-8** [automated] [VERIFIED] When an operator requests the audit history for a lifecycle request, the API shall return every audit event recorded against that request in ascending timestamp order, with no event omitted

## Criterion Assessment

2 of 8 criteria have potential quality issues:

**AC-REQ-010-2:**
- Info: Long criterion -- consider splitting into multiple focused criteria

**AC-REQ-010-3:**
- Info: Long criterion -- consider splitting into multiple focused criteria


## Recommended Test Types

### Acceptance / BDD
- **Scope:** Requirement-level behavior validation
- **Rationale:** 8 acceptance criteria defined

### Unit
- **Scope:** Individual function/method correctness
- **Rationale:** 24 source artifact(s) available for targeted testing

### Performance / Load
- **Scope:** Response time, throughput, resource usage
- **Rationale:** Non-functional requirement - likely needs performance validation

## Suggested Framework

**Framework:** Vitest
**Reason:** Modern Node.js test runner

## Architecture Components Under Test

| Component | Role | Technology |
|-----------|------|------------|
| Lifecycle API Service | backend-service | nodejs |

## Interfaces & Contracts to Verify

- **Lifecycle API Service** -> **Identity-Aware Proxy** via `rest` ("IAP Assertion Verification (JWK Set)") — schema present (oauth_oidc, 1090 chars, hash 233680e4)
- **Lifecycle API Service** -> **Cloud Logging: audit sink** via `dependency` ("Audit Log Sink") — dependency contract (no payload schema expected)
- **Lifecycle API Service** -> **Secret Manager** via `dependency` ("Secret Manager Access") — dependency contract (no payload schema expected)
- **Lifecycle API Service** -> **Cloud Tasks: lifecycle-steps** via `rest` ("Step Task Enqueue") — schema present (format unspecified, 2199 chars, hash 018db706)
- **External HTTPS Load Balancer** -> **Lifecycle API Service** via `rest` ("IAP-Protected HTTPS Ingress") — schema present (format unspecified, 2362 chars, hash 6b470d27)
- **Operator Console UI** -> **Lifecycle API Service** via `rest` ("Lifecycle Operator API") — schema present (openapi, 16222 chars, hash 0f700257)
- **Lifecycle API Service** -> **Firestore: lifecycle state and audit** via `nosql` ("Lifecycle State Store") — schema present (format unspecified, 5151 chars, hash 24f8907c)
- **Lifecycle API Service** -> **Lifecycle Step Executor** via `rest` ("Directory Lookup (read-only)") — schema present (openapi, 2935 chars, hash 7f4edbe8)

## Source Artifacts

| File | Kind | Language | Component |
|------|------|----------|-----------|
| `packages/shared/src/model.ts` | source | --- | Lifecycle API Service |
| `packages/shared/src/logging.ts` | source | --- | Lifecycle API Service |
| `packages/shared/src/dispatcher.ts` | source | --- | Lifecycle API Service |
| `services/api/src/schemas.ts` | source | --- | Lifecycle API Service |
| `services/api/src/index.ts` | source | --- | Lifecycle API Service |
| `services/api/src/authz.ts` | source | --- | Lifecycle API Service |
| `packages/shared/src/store.ts` | source | --- | Lifecycle API Service |
| `services/api/src/tasks/dispatcher.ts` | source | --- | Lifecycle API Service |
| `services/api/src/routes/requests.ts` | source | --- | Lifecycle API Service |
| `packages/shared/src/credentials.test.ts` | source | --- | Lifecycle API Service |
| `services/api/src/roles.ts` | source | --- | Lifecycle API Service |
| `services/api/src/middleware/iapAuth.ts` | source | --- | Lifecycle API Service |
| `services/api/src/routes/roleBindings.ts` | source | --- | Lifecycle API Service |
| `services/api/src/routes/audit.emulator.test.ts` | source | --- | Lifecycle API Service |
| `packages/shared/src/credentials.ts` | source | --- | Lifecycle API Service |
| `packages/shared/src/index.ts` | source | --- | Lifecycle API Service |
| `services/api/src/logging.ts` | source | --- | Lifecycle API Service |
| `packages/shared/src/policy.ts` | source | --- | Lifecycle API Service |
| `services/api/src/routes/admin.ts` | source | --- | Lifecycle API Service |
| `packages/shared/src/requestFactory.ts` | source | --- | Lifecycle API Service |
| `packages/shared/src/stepPlans.ts` | source | --- | Lifecycle API Service |
| `packages/shared/src/transitions.ts` | source | --- | Lifecycle API Service |
| `services/api/src/routes/credential.emulator.test.ts` | source | --- | Lifecycle API Service |
| `services/api/src/config.ts` | source | --- | Lifecycle API Service |

## Automated Test Scenarios

Derived section — regenerates when criteria, mappings, or contract schemas change; do not edit. Per scenario: derive the test from the cited criterion and the contract schemas, implement and run it, then report the outcome via report_test_results using the suggested test_id and the criterion's EXACT text as criterion_text (that binding is what flips met).

#### AC-REQ-010-1: each-audit-event-records-actor-identity-action-target-user-b
Derive the test from AC-REQ-010-1 and report the outcome with test_id "TC-REQ-010-1".

#### AC-REQ-010-2: when-an-operator-performs-an-approve-reject-cancel-resume-cr
Derive the test from AC-REQ-010-2 and report the outcome with test_id "TC-REQ-010-2".

#### AC-REQ-010-3: when-a-request-is-refused-with-401-by-assertion-verification
Derive the test from AC-REQ-010-3 and report the outcome with test_id "TC-REQ-010-3".

#### AC-REQ-010-4: when-an-automated-step-writes-an-audit-event-the-api-shall-r
Derive the test from AC-REQ-010-4 and report the outcome with test_id "TC-REQ-010-4".

#### AC-REQ-010-5: the-data-access-layer-exposes-no-update-or-delete-operation
Derive the test from AC-REQ-010-5 and report the outcome with test_id "TC-REQ-010-5".

#### AC-REQ-010-6: a-structured-logging-redaction-filter-strips-password-cipher
Derive the test from AC-REQ-010-6 and report the outcome with test_id "TC-REQ-010-6".

#### AC-REQ-010-7: when-any-audit-event-is-written-the-api-shall-persist-a-payl
Derive the test from AC-REQ-010-7 and report the outcome with test_id "TC-REQ-010-7".

#### AC-REQ-010-8: when-an-operator-requests-the-audit-history-for-a-lifecycle
Derive the test from AC-REQ-010-8 and report the outcome with test_id "TC-REQ-010-8".

## Contract Validation

Derived checks per contract — regenerates when schemas change; do not edit.

#### IAP Assertion Verification (JWK Set) (outbound: Identity-Aware Proxy)
- [ ] should verify IAP Assertion Verification (JWK Set) contract between Lifecycle API Service and Identity-Aware Proxy
- [ ] should handle Identity-Aware Proxy unavailability gracefully

#### Audit Log Sink (outbound: Cloud Logging: audit sink)
- [ ] should verify Audit Log Sink contract between Lifecycle API Service and Cloud Logging: audit sink
- [ ] should handle Cloud Logging: audit sink unavailability gracefully

#### Secret Manager Access (outbound: Secret Manager)
- [ ] should verify Secret Manager Access contract between Lifecycle API Service and Secret Manager
- [ ] should handle Secret Manager unavailability gracefully

#### Step Task Enqueue (outbound: Cloud Tasks: lifecycle-steps)
- [ ] should verify Step Task Enqueue contract between Lifecycle API Service and Cloud Tasks: lifecycle-steps
- [ ] should handle Cloud Tasks: lifecycle-steps unavailability gracefully

#### IAP-Protected HTTPS Ingress (outbound: Lifecycle API Service)
- [ ] should verify IAP-Protected HTTPS Ingress contract between External HTTPS Load Balancer and Lifecycle API Service
- [ ] should handle Lifecycle API Service unavailability gracefully

#### Lifecycle Operator API (outbound: Lifecycle API Service)
- [ ] should verify Lifecycle Operator API contract between Operator Console UI and Lifecycle API Service
- [ ] should handle Lifecycle API Service unavailability gracefully

#### Lifecycle State Store (outbound: Firestore: lifecycle state and audit)
- [ ] should verify Lifecycle State Store contract between Lifecycle API Service and Firestore: lifecycle state and audit
- [ ] should handle Firestore: lifecycle state and audit unavailability gracefully

#### Directory Lookup (read-only) (outbound: Lifecycle Step Executor)
- [ ] should verify Directory Lookup (read-only) contract between Lifecycle API Service and Lifecycle Step Executor
- [ ] should handle Lifecycle Step Executor unavailability gracefully

## Test Strategy

<!-- Edit this section to refine the testing approach -->

### Setup & Fixtures

- [ ] Define test data fixtures
- [ ] Set up mock services for external dependencies
- [ ] Configure test environment variables
