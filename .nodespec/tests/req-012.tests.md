# Test Plan: REQ-012 - Operator role model derived from verified identity

## Testing Objectives

**Requirement:** Operator role model derived from verified identity
**Category:** functional
**Description:** IAP proves who the caller is; the application decides what they may do. Operator identities from the verified assertion are mapped to application roles — requester (may draft and submit requests), approver (may approve or reject requests they did not create), admin (may edit approval policy, cancel or resume any request, and read the full audit trail) — through a role binding store keyed on the verified email, supporting both individual and Google-group-based bindings. Every API route declares its required role and is checked server-side on every call. Role assignment changes are themselves audited, and an operator with no binding is authenticated but authorized for nothing.

## Project Context

Company wants an application that supports the full user lifecycle in four phases:

1. User creation, including assignment of attributes and groups
2. Notifying the user of their new account with a welcome letter and initial password setting instructions
3. Updating a user's roles/attributes over time
4. Account deletion

Scope and constraints as stated by the customer:

- The application should o …

## Acceptance Criteria

Criterion text lives HERE once; every section below cites the AC id.

- **AC-REQ-012-1** [automated] [VERIFIED] Every API route declares a required role, and a call from an identity lacking it is rejected with 403 before the handler executes
- **AC-REQ-012-2** [automated] [VERIFIED] An authenticated identity with no role binding can read nothing and submit nothing — every route returns 403
- **AC-REQ-012-3** [automated] [VERIFIED] The requester role can create and submit requests but cannot approve any request, including requests created by others
- **AC-REQ-012-4** [automated] [VERIFIED] The approver role can approve requests created by others and is still refused approval of its own requests
- **AC-REQ-012-5** [automated] [PENDING] The admin role can edit approval policy, cancel or resume any request, and read the full audit trail
- **AC-REQ-012-6** [automated] [VERIFIED] Role binding changes write audit events recording the actor, the subject, and the before/after roles
- **AC-REQ-012-7** [automated] [VERIFIED] Group-based bindings resolve to the same effective permissions as an equivalent individual binding
- **AC-REQ-012-8** [automated] [VERIFIED] Every action the console offers is independently authorized server-side, verified by tests that call the API directly while bypassing the UI — hiding a control in the console is never the enforcement

## Criterion Assessment

2 of 8 criteria have potential quality issues:

**AC-REQ-012-7:**
- Warning: No clear trigger or action verb -- hard to derive a test case
- Suggestion: Rewrite in 'When [trigger], [system] shall [observable outcome]' form

**AC-REQ-012-8:**
- Warning: No clear trigger or action verb -- hard to derive a test case
- Suggestion: Rewrite in 'When [trigger], [system] shall [observable outcome]' form


## Recommended Test Types

### Acceptance / BDD
- **Scope:** Requirement-level behavior validation
- **Rationale:** 8 acceptance criteria defined

### Unit
- **Scope:** Individual function/method correctness
- **Rationale:** 19 source artifact(s) available for targeted testing

## Suggested Framework

**Framework:** Vitest
**Reason:** Modern Node.js test runner

## Architecture Components Under Test

| Component | Role | Technology |
|-----------|------|------------|
| Lifecycle API Service | backend-service | nodejs |

## Interfaces & Contracts to Verify

- **Lifecycle API Service** -> **Identity-Aware Proxy** via `rest` ("IAP Assertion Verification (JWK Set)") — schema present (oauth_oidc, 1090 chars, hash 233680e4)
- **Lifecycle API Service** -> **Cloud Logging: audit sink** via `dependency` ("Audit Log Sink") — dependency contract (no payload schema expected)
- **Lifecycle API Service** -> **Secret Manager** via `dependency` ("Secret Manager Access") — dependency contract (no payload schema expected)
- **Lifecycle API Service** -> **Cloud Tasks: lifecycle-steps** via `rest` ("Step Task Enqueue") — schema present (format unspecified, 2199 chars, hash 018db706)
- **External HTTPS Load Balancer** -> **Lifecycle API Service** via `rest` ("IAP-Protected HTTPS Ingress") — schema present (format unspecified, 2362 chars, hash 6b470d27)
- **Operator Console UI** -> **Lifecycle API Service** via `rest` ("Lifecycle Operator API") — schema present (openapi, 16222 chars, hash 0f700257)
- **Lifecycle API Service** -> **Firestore: lifecycle state and audit** via `nosql` ("Lifecycle State Store") — schema present (format unspecified, 5151 chars, hash 24f8907c)
- **Lifecycle API Service** -> **Lifecycle Step Executor** via `rest` ("Directory Lookup (read-only)") — schema present (openapi, 2935 chars, hash 7f4edbe8)

## Source Artifacts

| File | Kind | Language | Component |
|------|------|----------|-----------|
| `packages/shared/src/model.ts` | source | --- | Lifecycle API Service |
| `packages/shared/src/logging.ts` | source | --- | Lifecycle API Service |
| `packages/shared/src/dispatcher.ts` | source | --- | Lifecycle API Service |
| `services/api/src/schemas.ts` | source | --- | Lifecycle API Service |
| `services/api/src/index.ts` | source | --- | Lifecycle API Service |
| `services/api/src/authz.ts` | source | --- | Lifecycle API Service |
| `packages/shared/src/store.ts` | source | --- | Lifecycle API Service |
| `services/api/src/tasks/dispatcher.ts` | source | --- | Lifecycle API Service |
| `services/api/src/routes/requests.ts` | source | --- | Lifecycle API Service |
| `services/api/src/roles.ts` | source | --- | Lifecycle API Service |
| `services/api/src/middleware/iapAuth.ts` | source | --- | Lifecycle API Service |
| `services/api/src/routes/roleBindings.ts` | source | --- | Lifecycle API Service |
| `packages/shared/src/index.ts` | source | --- | Lifecycle API Service |
| `services/api/src/logging.ts` | source | --- | Lifecycle API Service |
| `packages/shared/src/policy.ts` | source | --- | Lifecycle API Service |
| `packages/shared/src/requestFactory.ts` | source | --- | Lifecycle API Service |
| `packages/shared/src/stepPlans.ts` | source | --- | Lifecycle API Service |
| `packages/shared/src/transitions.ts` | source | --- | Lifecycle API Service |
| `services/api/src/config.ts` | source | --- | Lifecycle API Service |

## Automated Test Scenarios

Derived section — regenerates when criteria, mappings, or contract schemas change; do not edit. Per scenario: derive the test from the cited criterion and the contract schemas, implement and run it, then report the outcome via report_test_results using the suggested test_id and the criterion's EXACT text as criterion_text (that binding is what flips met).

#### AC-REQ-012-1: every-api-route-declares-a-required-role-and-a-call-from-an
Derive the test from AC-REQ-012-1 and report the outcome with test_id "TC-REQ-012-1".

#### AC-REQ-012-2: an-authenticated-identity-with-no-role-binding-can-read-noth
Derive the test from AC-REQ-012-2 and report the outcome with test_id "TC-REQ-012-2".

#### AC-REQ-012-3: the-requester-role-can-create-and-submit-requests-but-cannot
Derive the test from AC-REQ-012-3 and report the outcome with test_id "TC-REQ-012-3".

#### AC-REQ-012-4: the-approver-role-can-approve-requests-created-by-others-and
Derive the test from AC-REQ-012-4 and report the outcome with test_id "TC-REQ-012-4".

#### AC-REQ-012-5: the-admin-role-can-edit-approval-policy-cancel-or-resume-any
Derive the test from AC-REQ-012-5 and report the outcome with test_id "TC-REQ-012-5".

#### AC-REQ-012-6: role-binding-changes-write-audit-events-recording-the-actor
Derive the test from AC-REQ-012-6 and report the outcome with test_id "TC-REQ-012-6".

#### AC-REQ-012-7: group-based-bindings-resolve-to-the-same-effective-permissio
Derive the test from AC-REQ-012-7 and report the outcome with test_id "TC-REQ-012-7".

#### AC-REQ-012-8: every-action-the-console-offers-is-independently-authorized
Derive the test from AC-REQ-012-8 and report the outcome with test_id "TC-REQ-012-8".

## Contract Validation

Derived checks per contract — regenerates when schemas change; do not edit.

#### IAP Assertion Verification (JWK Set) (outbound: Identity-Aware Proxy)
- [ ] should verify IAP Assertion Verification (JWK Set) contract between Lifecycle API Service and Identity-Aware Proxy
- [ ] should handle Identity-Aware Proxy unavailability gracefully

#### Audit Log Sink (outbound: Cloud Logging: audit sink)
- [ ] should verify Audit Log Sink contract between Lifecycle API Service and Cloud Logging: audit sink
- [ ] should handle Cloud Logging: audit sink unavailability gracefully

#### Secret Manager Access (outbound: Secret Manager)
- [ ] should verify Secret Manager Access contract between Lifecycle API Service and Secret Manager
- [ ] should handle Secret Manager unavailability gracefully

#### Step Task Enqueue (outbound: Cloud Tasks: lifecycle-steps)
- [ ] should verify Step Task Enqueue contract between Lifecycle API Service and Cloud Tasks: lifecycle-steps
- [ ] should handle Cloud Tasks: lifecycle-steps unavailability gracefully

#### IAP-Protected HTTPS Ingress (outbound: Lifecycle API Service)
- [ ] should verify IAP-Protected HTTPS Ingress contract between External HTTPS Load Balancer and Lifecycle API Service
- [ ] should handle Lifecycle API Service unavailability gracefully

#### Lifecycle Operator API (outbound: Lifecycle API Service)
- [ ] should verify Lifecycle Operator API contract between Operator Console UI and Lifecycle API Service
- [ ] should handle Lifecycle API Service unavailability gracefully

#### Lifecycle State Store (outbound: Firestore: lifecycle state and audit)
- [ ] should verify Lifecycle State Store contract between Lifecycle API Service and Firestore: lifecycle state and audit
- [ ] should handle Firestore: lifecycle state and audit unavailability gracefully

#### Directory Lookup (read-only) (outbound: Lifecycle Step Executor)
- [ ] should verify Directory Lookup (read-only) contract between Lifecycle API Service and Lifecycle Step Executor
- [ ] should handle Lifecycle Step Executor unavailability gracefully

## Test Strategy

<!-- Edit this section to refine the testing approach -->

### Setup & Fixtures

- [ ] Define test data fixtures
- [ ] Set up mock services for external dependencies
- [ ] Configure test environment variables
