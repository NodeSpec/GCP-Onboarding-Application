# Test Plan: REQ-005 - Phase 3 — Role and attribute updates over time

## Testing Objectives

**Requirement:** Phase 3 — Role and attribute updates over time
**Category:** functional
**Description:** An existing user's attributes, org unit, and group memberships can be changed after onboarding. The update request is expressed as a desired-state diff: the operator submits the fields to change and the groups to add/remove, and the app computes the concrete change set against the user's live Workspace state at execution time. The computed diff is shown for approval before execution when the step requires approval, so the approver sees exactly what will change rather than a raw payload. Updates use Admin SDK users.update / users.patch and members.insert / members.delete, each group change as its own idempotent step so partial failure is recoverable. A change that would be a no-op against live state is recorded as 'skipped' rather than executed.

RECORDED INTERPRETATION — what "roles" means. The customer's phase 3 asks to update "a user's roles/attributes over time". In Google Workspace "role" can mean two very different things: an access grouping (group membership), or a Workspace ADMIN role assigned through roleAssignments. This requirement reads it as the former, for three reasons.

First, coherence: phase 1 grants groups at creation and phase 3 is the only phase that changes anything afterwards, so if "roles" excluded groups, group membership would be write-once — which no user-lifecycle tool can be, and which would leave the customer's own four phases unable to close.

Second, the scenario the phrase describes: "over time" is the mover case, someone changing team or job. In Workspace that is expressed as group membership plus org unit and job attributes. Administrative privilege is not what changes when a person moves teams.

Third, and decisively, admin-role management is a privilege-escalation surface. A service account able to assign admin roles can grant itself — or anyone else — Super Admin. The customer explicitly refused Domain-Wide Delegation to keep this integration's blast radius small; handing the same integration the ability to mint administrators would undo that, and as a side effect of routine attribute updates rather than a deliberate decision.

So "roles" here means group memberships, together with the attributes that describe a role: job title, department, manager, and org unit path. Assigning or revoking WORKSPACE ADMIN roles is out of scope, and the custom admin role deliberately lacks the privilege (REQ-027). If Company does want admin-role management, it is an additive change — one additional scope, one additional privilege on the custom admin role, and an explicit acceptance of the escalation surface — and it should be a conversation, not an assumption inherited from an ambiguous word.

## Project Context

Company wants an application that supports the full user lifecycle in four phases:

1. User creation, including assignment of attributes and groups
2. Notifying the user of their new account with a welcome letter and initial password setting instructions
3. Updating a user's roles/attributes over time
4. Account deletion

Scope and constraints as stated by the customer:

- The application should o …

## Acceptance Criteria

Criterion text lives HERE once; every section below cites the AC id.

- **AC-REQ-005-1** [automated] [VERIFIED] Submitting an update request computes a diff against the user's live Workspace state and persists it on the request before execution
- **AC-REQ-005-2** [automated] [VERIFIED] The rendered approval payload shows the before/after value of every changed attribute and every group being added or removed
- **AC-REQ-005-3** [automated] [VERIFIED] Applying the update changes exactly the attributes and memberships in the diff and leaves all other user state untouched
- **AC-REQ-005-4** [automated] [VERIFIED] Changing a user's role is expressed as group membership changes plus role-describing attributes — job title, department, manager and org unit path — and all of these are updatable through this phase
- **AC-REQ-005-5** [automated] [VERIFIED] A requested change that already matches live state is recorded as step status 'skipped' and issues no Workspace call
- **AC-REQ-005-6** [automated] [VERIFIED] Removing a group membership the user does not have is treated as already-satisfied ('skipped'), not as an error
- **AC-REQ-005-7** [automated] [VERIFIED] If one group change fails, the other group changes that succeeded are retained and the failing change is reported on its own step
- **AC-REQ-005-8** [automated] [VERIFIED] An update request targeting a non-existent or suspended-and-deleted user fails validation before any mutation
- **AC-REQ-005-9** [automated] [VERIFIED] Workspace ADMIN role assignment is unreachable through any phase — a repository check finds no roleAssignments API call and no admin.directory.rolemanagement scope anywhere in the codebase or IaC

## Criterion Assessment

5 of 9 criteria have potential quality issues:

**AC-REQ-005-3:**
- Warning: No clear trigger or action verb -- hard to derive a test case
- Suggestion: Rewrite in 'When [trigger], [system] shall [observable outcome]' form

**AC-REQ-005-4:**
- Warning: No clear trigger or action verb -- hard to derive a test case
- Suggestion: Rewrite in 'When [trigger], [system] shall [observable outcome]' form

**AC-REQ-005-5:**
- Warning: No clear trigger or action verb -- hard to derive a test case
- Suggestion: Rewrite in 'When [trigger], [system] shall [observable outcome]' form

**AC-REQ-005-6:**
- Warning: No clear trigger or action verb -- hard to derive a test case
- Suggestion: Rewrite in 'When [trigger], [system] shall [observable outcome]' form

**AC-REQ-005-9:**
- Warning: No clear trigger or action verb -- hard to derive a test case
- Suggestion: Rewrite in 'When [trigger], [system] shall [observable outcome]' form


## Recommended Test Types

### Acceptance / BDD
- **Scope:** Requirement-level behavior validation
- **Rationale:** 9 acceptance criteria defined

### Unit
- **Scope:** Individual function/method correctness
- **Rationale:** 24 source artifact(s) available for targeted testing

## Suggested Framework

**Framework:** Vitest
**Reason:** Modern Node.js test runner

## Architecture Components Under Test

| Component | Role | Technology |
|-----------|------|------------|
| Lifecycle Step Executor | worker | nodejs |

## Interfaces & Contracts to Verify

- **Lifecycle Step Executor** -> **Cloud Tasks: lifecycle-steps** via `rest` ("Step Task Enqueue") — schema present (format unspecified, 2199 chars, hash 018db706)
- **Lifecycle Step Executor** -> **Firestore: lifecycle state and audit** via `nosql` ("Lifecycle State Store") — schema present (format unspecified, 5151 chars, hash 24f8907c)
- **Lifecycle Step Executor** -> **Secret Manager** via `dependency` ("Secret Manager Access") — dependency contract (no payload schema expected)
- **Cloud Tasks: lifecycle-steps** -> **Lifecycle Step Executor** via `rest` ("Step Execution Dispatch") — schema present (openapi, 5398 chars, hash 244e43cb)
- **Lifecycle Step Executor** -> **Cloud Logging: audit sink** via `dependency` ("Audit Log Sink") — dependency contract (no payload schema expected)
- **Lifecycle Step Executor** -> **Email Delivery Service** via `rest` ("Welcome Letter Delivery") — schema present (format unspecified, 4322 chars, hash 11297bdc)
- **Lifecycle Step Executor** -> **Google Workspace (Admin SDK Directory)** via `rest` ("Google Admin SDK Directory API") — schema present (openapi, 3927 chars, hash 055634f4)
- **Lifecycle API Service** -> **Lifecycle Step Executor** via `rest` ("Directory Lookup (read-only)") — schema present (openapi, 2935 chars, hash 7f4edbe8)

## Source Artifacts

| File | Kind | Language | Component |
|------|------|----------|-----------|
| `services/worker/src/notify/approvers.ts` | source | --- | Lifecycle Step Executor |
| `services/worker/src/phases/notify.ts` | source | --- | Lifecycle Step Executor |
| `services/worker/src/notify/sender.ts` | source | --- | Lifecycle Step Executor |
| `services/worker/src/notify/notify.emulator.test.ts` | source | --- | Lifecycle Step Executor |
| `services/worker/src/tasks/dispatcher.ts` | source | --- | Lifecycle Step Executor |
| `services/worker/src/phases/update.ts` | source | --- | Lifecycle Step Executor |
| `services/worker/src/auth/taskAuth.ts` | source | --- | Lifecycle Step Executor |
| `services/worker/src/config.ts` | source | --- | Lifecycle Step Executor |
| `services/worker/src/steps/approvalExpiry.emulator.test.ts` | source | --- | Lifecycle Step Executor |
| `services/worker/src/phases/createCollision.emulator.test.ts` | source | --- | Lifecycle Step Executor |
| `services/worker/src/steps/handler.ts` | source | --- | Lifecycle Step Executor |
| `services/worker/src/steps/duplicateDelivery.emulator.test.ts` | source | --- | Lifecycle Step Executor |
| `services/worker/src/index.ts` | source | --- | Lifecycle Step Executor |
| `services/worker/src/workspace/passwordReset.test.ts` | source | --- | Lifecycle Step Executor |
| `services/worker/src/routes/tasks.ts` | source | --- | Lifecycle Step Executor |
| `services/worker/src/phases/create.ts` | source | --- | Lifecycle Step Executor |
| `services/worker/src/notify/resend.emulator.test.ts` | source | --- | Lifecycle Step Executor |
| `services/worker/src/steps/advance.ts` | source | --- | Lifecycle Step Executor |
| `services/worker/src/workspace/directoryClient.ts` | source | --- | Lifecycle Step Executor |
| `services/worker/src/workspace/passwordPolicy.test.ts` | source | --- | Lifecycle Step Executor |
| `services/worker/src/phases/credentialExposure.emulator.test.ts` | source | --- | Lifecycle Step Executor |
| `services/worker/src/logging.ts` | source | --- | Lifecycle Step Executor |
| `services/worker/src/notify/templates.ts` | source | --- | Lifecycle Step Executor |
| `services/worker/src/steps/executor.ts` | source | --- | Lifecycle Step Executor |

## Automated Test Scenarios

Derived section — regenerates when criteria, mappings, or contract schemas change; do not edit. Per scenario: derive the test from the cited criterion and the contract schemas, implement and run it, then report the outcome via report_test_results using the suggested test_id and the criterion's EXACT text as criterion_text (that binding is what flips met).

#### AC-REQ-005-1: submitting-an-update-request-computes-a-diff-against-the-use
Derive the test from AC-REQ-005-1 and report the outcome with test_id "TC-REQ-005-1".

#### AC-REQ-005-2: the-rendered-approval-payload-shows-the-before-after-value-o
Derive the test from AC-REQ-005-2 and report the outcome with test_id "TC-REQ-005-2".

#### AC-REQ-005-3: applying-the-update-changes-exactly-the-attributes-and-membe
Derive the test from AC-REQ-005-3 and report the outcome with test_id "TC-REQ-005-3".

#### AC-REQ-005-4: changing-a-user-s-role-is-expressed-as-group-membership-chan
Derive the test from AC-REQ-005-4 and report the outcome with test_id "TC-REQ-005-4".

#### AC-REQ-005-5: a-requested-change-that-already-matches-live-state-is-record
Derive the test from AC-REQ-005-5 and report the outcome with test_id "TC-REQ-005-5".

#### AC-REQ-005-6: removing-a-group-membership-the-user-does-not-have-is-treate
Derive the test from AC-REQ-005-6 and report the outcome with test_id "TC-REQ-005-6".

#### AC-REQ-005-7: if-one-group-change-fails-the-other-group-changes-that-succe
Derive the test from AC-REQ-005-7 and report the outcome with test_id "TC-REQ-005-7".

#### AC-REQ-005-8: an-update-request-targeting-a-non-existent-or-suspended-and
Derive the test from AC-REQ-005-8 and report the outcome with test_id "TC-REQ-005-8".

#### AC-REQ-005-9: workspace-admin-role-assignment-is-unreachable-through-any-p
Derive the test from AC-REQ-005-9 and report the outcome with test_id "TC-REQ-005-9".

## Contract Validation

Derived checks per contract — regenerates when schemas change; do not edit.

#### Step Task Enqueue (outbound: Cloud Tasks: lifecycle-steps)
- [ ] should verify Step Task Enqueue contract between Lifecycle Step Executor and Cloud Tasks: lifecycle-steps
- [ ] should handle Cloud Tasks: lifecycle-steps unavailability gracefully

#### Lifecycle State Store (outbound: Firestore: lifecycle state and audit)
- [ ] should verify Lifecycle State Store contract between Lifecycle Step Executor and Firestore: lifecycle state and audit
- [ ] should handle Firestore: lifecycle state and audit unavailability gracefully

#### Secret Manager Access (outbound: Secret Manager)
- [ ] should verify Secret Manager Access contract between Lifecycle Step Executor and Secret Manager
- [ ] should handle Secret Manager unavailability gracefully

#### Step Execution Dispatch (outbound: Lifecycle Step Executor)
- [ ] should verify Step Execution Dispatch contract between Cloud Tasks: lifecycle-steps and Lifecycle Step Executor
- [ ] should handle Lifecycle Step Executor unavailability gracefully

#### Audit Log Sink (outbound: Cloud Logging: audit sink)
- [ ] should verify Audit Log Sink contract between Lifecycle Step Executor and Cloud Logging: audit sink
- [ ] should handle Cloud Logging: audit sink unavailability gracefully

#### Welcome Letter Delivery (outbound: Email Delivery Service)
- [ ] should verify Welcome Letter Delivery contract between Lifecycle Step Executor and Email Delivery Service
- [ ] should handle Email Delivery Service unavailability gracefully

#### Google Admin SDK Directory API (outbound: Google Workspace (Admin SDK Directory))
- [ ] should verify Google Admin SDK Directory API contract between Lifecycle Step Executor and Google Workspace (Admin SDK Directory)
- [ ] should handle Google Workspace (Admin SDK Directory) unavailability gracefully

#### Directory Lookup (read-only) (outbound: Lifecycle Step Executor)
- [ ] should verify Directory Lookup (read-only) contract between Lifecycle API Service and Lifecycle Step Executor
- [ ] should handle Lifecycle Step Executor unavailability gracefully

## Test Strategy

<!-- Edit this section to refine the testing approach -->

### Setup & Fixtures

- [ ] Define test data fixtures
- [ ] Set up mock services for external dependencies
- [ ] Configure test environment variables
