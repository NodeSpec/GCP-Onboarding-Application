# Test Plan: REQ-007 - IAP protection with server-side JWT assertion verification

## Testing Objectives

**Requirement:** IAP protection with server-side JWT assertion verification
**Category:** technical
**Description:** The customer's constraint is that the serverless application is protected by IAP and verifies the JWT header so the security controls cannot be bypassed. This requirement states exactly how far that protection reaches across the deployed services, so the coverage is auditable rather than assumed. There are no exceptions: the system exposes no unauthenticated route of any kind.

OPERATOR SURFACE (lifecycle-api, serving the console and the operator API): the only load-balancer backend service in the system. IAP is enabled on it, and the application independently verifies IAP's signed assertion on every request rather than trusting the perimeter. Middleware reads the x-goog-iap-jwt-assertion header, verifies the ES256 signature against Google's IAP public JWK set (cached with refresh on unknown kid), and checks issuer == https://cloud.google.com/iap, the exact expected audience string /projects/PROJECT_NUMBER/global/backendServices/BACKEND_SERVICE_ID, and exp/iat within tolerance. The verified email and sub claims become the request identity; no client-supplied header or body field may ever set identity. A request without a valid assertion is rejected with 401 before any route handler runs — fail closed.

WORKER (lifecycle-worker): not IAP-protected, and deliberately so — IAP is a control on human access, and the worker has no human ingress path. It is not attached to the load balancer at all. It is protected by Cloud Run ingress restricted to internal-and-cloud-load-balancing, run.invoker granted only to the Cloud Tasks queue's invoker service account, and a required OIDC token on every request. This is stated explicitly so that "not behind IAP" is a documented, tested property rather than something the customer discovers.

Across both services, Cloud Run ingress is restricted to internal-and-cloud-load-balancing and run.invoker is granted only to the appropriate caller identity, so a direct *.run.app request cannot reach either of them.

There is no end-user-facing surface anywhere in this system. Users being onboarded never interact with the application; they receive a letter and use Google's own sign-in and password-change flow. That is what allows total IAP coverage, and any future feature that would require an unauthenticated route is a change to this requirement and needs the customer's agreement first.

## Project Context

Company wants an application that supports the full user lifecycle in four phases:

1. User creation, including assignment of attributes and groups
2. Notifying the user of their new account with a welcome letter and initial password setting instructions
3. Updating a user's roles/attributes over time
4. Account deletion

Scope and constraints as stated by the customer:

- The application should o …

## Acceptance Criteria

Criterion text lives HERE once; every section below cites the AC id.

- **AC-REQ-007-1** [automated] [VERIFIED] Every operator-facing route rejects a request carrying no x-goog-iap-jwt-assertion header with 401, and no route handler is invoked
- **AC-REQ-007-2** [automated] [VERIFIED] A request with a syntactically valid but wrongly-signed assertion is rejected with 401
- **AC-REQ-007-3** [automated] [VERIFIED] A request whose assertion carries a different audience string than the configured backend-service audience is rejected with 401
- **AC-REQ-007-4** [automated] [VERIFIED] A request whose assertion carries an issuer other than https://cloud.google.com/iap is rejected with 401
- **AC-REQ-007-5** [automated] [VERIFIED] An expired assertion (exp in the past beyond the configured skew) is rejected with 401
- **AC-REQ-007-6** [automated] [VERIFIED] A valid assertion populates the request identity from the token's email and sub claims, and a client-supplied identity header or body field is ignored and never overrides it
- **AC-REQ-007-7** [automated] [VERIFIED] An unknown key id triggers a JWK set refresh and the request succeeds if the refreshed set contains the key; a still-unknown kid is rejected with 401
- **AC-REQ-007-8** [automated] [VERIFIED] Verification failures are logged with the reason and source IP but never log the raw assertion
- **AC-REQ-007-9** [automated] [PENDING] Each deployed Cloud Run service rejects a direct request to its *.run.app URL, proving the load balancer is the only ingress path into the system
- **AC-REQ-007-10** [automated] [VERIFIED] The lifecycle-worker service admits exactly two caller identities, each confined to its own route class: the Cloud Tasks queue invoker on /tasks/*, and the lifecycle-api service account on /lookup/*. A token issued to either identity is rejected with 401 on the other's routes, and an unauthenticated request is rejected on both
- **AC-REQ-007-11** [automated] [VERIFIED] Every load-balancer backend service in the deployment has IAP enabled — asserted against the committed Terraform, so a backend added without IAP fails the check
- **AC-REQ-007-12** [automated] [PENDING] The deployed system exposes no unauthenticated route: an unauthenticated request to every path in the application's route table is rejected, enumerated as a test rather than spot-checked
- **AC-REQ-007-13** [manual] [PENDING] IAP is enabled on the operator backend service and access is granted only to the intended operator group in the OAuth/IAM configuration

## Criterion Assessment

11 of 13 criteria have potential quality issues:

**AC-REQ-007-1:**
- Warning: No clear trigger or action verb -- hard to derive a test case
- Suggestion: Rewrite in 'When [trigger], [system] shall [observable outcome]' form

**AC-REQ-007-2:**
- Warning: No clear trigger or action verb -- hard to derive a test case
- Suggestion: Rewrite in 'When [trigger], [system] shall [observable outcome]' form

**AC-REQ-007-3:**
- Warning: No clear trigger or action verb -- hard to derive a test case
- Suggestion: Rewrite in 'When [trigger], [system] shall [observable outcome]' form

**AC-REQ-007-4:**
- Warning: No clear trigger or action verb -- hard to derive a test case
- Suggestion: Rewrite in 'When [trigger], [system] shall [observable outcome]' form

**AC-REQ-007-5:**
- Warning: No clear trigger or action verb -- hard to derive a test case
- Suggestion: Rewrite in 'When [trigger], [system] shall [observable outcome]' form

**AC-REQ-007-6:**
- Warning: No clear trigger or action verb -- hard to derive a test case
- Suggestion: Rewrite in 'When [trigger], [system] shall [observable outcome]' form

**AC-REQ-007-8:**
- Warning: No clear trigger or action verb -- hard to derive a test case
- Suggestion: Rewrite in 'When [trigger], [system] shall [observable outcome]' form

**AC-REQ-007-9:**
- Warning: No clear trigger or action verb -- hard to derive a test case
- Suggestion: Rewrite in 'When [trigger], [system] shall [observable outcome]' form

**AC-REQ-007-10:**
- Info: Long criterion -- consider splitting into multiple focused criteria

**AC-REQ-007-11:**
- Warning: No clear trigger or action verb -- hard to derive a test case
- Suggestion: Rewrite in 'When [trigger], [system] shall [observable outcome]' form

**AC-REQ-007-12:**
- Warning: No clear trigger or action verb -- hard to derive a test case
- Suggestion: Rewrite in 'When [trigger], [system] shall [observable outcome]' form


## Recommended Test Types

### Acceptance / BDD
- **Scope:** Requirement-level behavior validation
- **Rationale:** 13 acceptance criteria defined

### Unit
- **Scope:** Individual function/method correctness
- **Rationale:** 27 source artifact(s) available for targeted testing

## Suggested Framework

**Framework:** Vitest
**Reason:** Modern Node.js test runner

## Architecture Components Under Test

| Component | Role | Technology |
|-----------|------|------------|
| Lifecycle API Service | backend-service | nodejs |

## Interfaces & Contracts to Verify

- **Lifecycle API Service** -> **Identity-Aware Proxy** via `rest` ("IAP Assertion Verification (JWK Set)") — schema present (oauth_oidc, 1090 chars, hash 233680e4)
- **Lifecycle API Service** -> **Cloud Logging: audit sink** via `dependency` ("Audit Log Sink") — dependency contract (no payload schema expected)
- **Lifecycle API Service** -> **Secret Manager** via `dependency` ("Secret Manager Access") — dependency contract (no payload schema expected)
- **Lifecycle API Service** -> **Cloud Tasks: lifecycle-steps** via `rest` ("Step Task Enqueue") — schema present (format unspecified, 2199 chars, hash 018db706)
- **External HTTPS Load Balancer** -> **Lifecycle API Service** via `rest` ("IAP-Protected HTTPS Ingress") — schema present (format unspecified, 2362 chars, hash 6b470d27)
- **Operator Console UI** -> **Lifecycle API Service** via `rest` ("Lifecycle Operator API") — schema present (openapi, 16222 chars, hash 0f700257)
- **Lifecycle API Service** -> **Firestore: lifecycle state and audit** via `nosql` ("Lifecycle State Store") — schema present (format unspecified, 5151 chars, hash 24f8907c)
- **Lifecycle API Service** -> **Lifecycle Step Executor** via `rest` ("Directory Lookup (read-only)") — schema present (openapi, 2935 chars, hash 7f4edbe8)

## Source Artifacts

| File | Kind | Language | Component |
|------|------|----------|-----------|
| `packages/shared/src/model.ts` | source | --- | Lifecycle API Service |
| `packages/shared/src/logging.ts` | source | --- | Lifecycle API Service |
| `packages/shared/src/dispatcher.ts` | source | --- | Lifecycle API Service |
| `packages/schemas/src/index.ts` | source | --- | Lifecycle API Service |
| `services/api/src/index.ts` | source | --- | Lifecycle API Service |
| `services/api/src/authz.ts` | source | --- | Lifecycle API Service |
| `packages/shared/src/store.ts` | source | --- | Lifecycle API Service |
| `services/api/src/tasks/dispatcher.ts` | source | --- | Lifecycle API Service |
| `services/api/src/routes/requests.ts` | source | --- | Lifecycle API Service |
| `packages/shared/src/credentials.test.ts` | source | --- | Lifecycle API Service |
| `services/api/src/roles.ts` | source | --- | Lifecycle API Service |
| `packages/shared/src/policy.ts` | source | --- | Lifecycle API Service |
| `services/api/src/middleware/iapAuth.ts` | source | --- | Lifecycle API Service |
| `services/api/src/routes/roleBindings.ts` | source | --- | Lifecycle API Service |
| `services/api/src/routes/audit.emulator.test.ts` | source | --- | Lifecycle API Service |
| `packages/shared/src/credentials.ts` | source | --- | Lifecycle API Service |
| `services/api/src/lookup/workerClient.ts` | source | --- | Lifecycle API Service |
| `packages/shared/src/index.ts` | source | --- | Lifecycle API Service |
| `services/api/src/logging.ts` | source | --- | Lifecycle API Service |
| `services/api/src/routes/admin.ts` | source | --- | Lifecycle API Service |
| `packages/shared/src/requestFactory.ts` | source | --- | Lifecycle API Service |
| `services/api/src/protectedAccounts.ts` | source | --- | Lifecycle API Service |
| `packages/shared/src/stepPlans.ts` | source | --- | Lifecycle API Service |
| `packages/shared/src/transitions.ts` | source | --- | Lifecycle API Service |
| `services/api/src/routes/credential.emulator.test.ts` | source | --- | Lifecycle API Service |
| `services/api/src/routes/lookup.ts` | source | --- | Lifecycle API Service |
| `services/api/src/config.ts` | source | --- | Lifecycle API Service |

## Automated Test Scenarios

Derived section — regenerates when criteria, mappings, or contract schemas change; do not edit. Per scenario: derive the test from the cited criterion and the contract schemas, implement and run it, then report the outcome via report_test_results using the suggested test_id and the criterion's EXACT text as criterion_text (that binding is what flips met).

#### AC-REQ-007-1: every-operator-facing-route-rejects-a-request-carrying-no-x
Derive the test from AC-REQ-007-1 and report the outcome with test_id "TC-REQ-007-1".

#### AC-REQ-007-2: a-request-with-a-syntactically-valid-but-wrongly-signed-asse
Derive the test from AC-REQ-007-2 and report the outcome with test_id "TC-REQ-007-2".

#### AC-REQ-007-3: a-request-whose-assertion-carries-a-different-audience-strin
Derive the test from AC-REQ-007-3 and report the outcome with test_id "TC-REQ-007-3".

#### AC-REQ-007-4: a-request-whose-assertion-carries-an-issuer-other-than-https
Derive the test from AC-REQ-007-4 and report the outcome with test_id "TC-REQ-007-4".

#### AC-REQ-007-5: an-expired-assertion-exp-in-the-past-beyond-the-configured-s
Derive the test from AC-REQ-007-5 and report the outcome with test_id "TC-REQ-007-5".

#### AC-REQ-007-6: a-valid-assertion-populates-the-request-identity-from-the-to
Derive the test from AC-REQ-007-6 and report the outcome with test_id "TC-REQ-007-6".

#### AC-REQ-007-7: an-unknown-key-id-triggers-a-jwk-set-refresh-and-the-request
Derive the test from AC-REQ-007-7 and report the outcome with test_id "TC-REQ-007-7".

#### AC-REQ-007-8: verification-failures-are-logged-with-the-reason-and-source
Derive the test from AC-REQ-007-8 and report the outcome with test_id "TC-REQ-007-8".

#### AC-REQ-007-9: each-deployed-cloud-run-service-rejects-a-direct-request-to
Derive the test from AC-REQ-007-9 and report the outcome with test_id "TC-REQ-007-9".

#### AC-REQ-007-10: the-lifecycle-worker-service-admits-exactly-two-caller-ident
Derive the test from AC-REQ-007-10 and report the outcome with test_id "TC-REQ-007-10".

#### AC-REQ-007-11: every-load-balancer-backend-service-in-the-deployment-has-ia
Derive the test from AC-REQ-007-11 and report the outcome with test_id "TC-REQ-007-11".

#### AC-REQ-007-12: the-deployed-system-exposes-no-unauthenticated-route-an-unau
Derive the test from AC-REQ-007-12 and report the outcome with test_id "TC-REQ-007-12".

## Contract Validation

Derived checks per contract — regenerates when schemas change; do not edit.

#### IAP Assertion Verification (JWK Set) (outbound: Identity-Aware Proxy)
- [ ] should verify IAP Assertion Verification (JWK Set) contract between Lifecycle API Service and Identity-Aware Proxy
- [ ] should handle Identity-Aware Proxy unavailability gracefully

#### Audit Log Sink (outbound: Cloud Logging: audit sink)
- [ ] should verify Audit Log Sink contract between Lifecycle API Service and Cloud Logging: audit sink
- [ ] should handle Cloud Logging: audit sink unavailability gracefully

#### Secret Manager Access (outbound: Secret Manager)
- [ ] should verify Secret Manager Access contract between Lifecycle API Service and Secret Manager
- [ ] should handle Secret Manager unavailability gracefully

#### Step Task Enqueue (outbound: Cloud Tasks: lifecycle-steps)
- [ ] should verify Step Task Enqueue contract between Lifecycle API Service and Cloud Tasks: lifecycle-steps
- [ ] should handle Cloud Tasks: lifecycle-steps unavailability gracefully

#### IAP-Protected HTTPS Ingress (outbound: Lifecycle API Service)
- [ ] should verify IAP-Protected HTTPS Ingress contract between External HTTPS Load Balancer and Lifecycle API Service
- [ ] should handle Lifecycle API Service unavailability gracefully

#### Lifecycle Operator API (outbound: Lifecycle API Service)
- [ ] should verify Lifecycle Operator API contract between Operator Console UI and Lifecycle API Service
- [ ] should handle Lifecycle API Service unavailability gracefully

#### Lifecycle State Store (outbound: Firestore: lifecycle state and audit)
- [ ] should verify Lifecycle State Store contract between Lifecycle API Service and Firestore: lifecycle state and audit
- [ ] should handle Firestore: lifecycle state and audit unavailability gracefully

#### Directory Lookup (read-only) (outbound: Lifecycle Step Executor)
- [ ] should verify Directory Lookup (read-only) contract between Lifecycle API Service and Lifecycle Step Executor
- [ ] should handle Lifecycle Step Executor unavailability gracefully

## Manual Verification

These criteria are proven by a HUMAN: perform the check, tick the criterion box in the owning node's task document, and have the user approve the resulting change card — that approval is what flips met. report_test_results REFUSES to bind manual criteria; do not author test cases for them.

- [ ] AC-REQ-007-13 — verify per the criterion text above, then tick + approve via the task doc

## Test Strategy

<!-- Edit this section to refine the testing approach -->

### Setup & Fixtures

- [ ] Define test data fixtures
- [ ] Set up mock services for external dependencies
- [ ] Configure test environment variables
